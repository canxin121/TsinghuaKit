# TsinghuaKit Rust crate

This directory contains the `tsinghua_kit` Cargo package. The root Flutter plugin invokes Cargokit to build this crate for each native target.

Build the library with:

```sh
cargo build --manifest-path rust/Cargo.toml --locked
```
